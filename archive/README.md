# Archivos Históricos (`archive/`)

**¡ATENCIÓN!**
Los archivos en esta carpeta corresponden a versiones antiguas o respaldos históricos (anteriormente en `src_backup/`). 

**No son la fuente de verdad actual.** 
Para ver o modificar el código real del bot que se está desplegando, ve a la carpeta `src/`.

Esta carpeta se mantiene únicamente por fines de consulta histórica.
